# QR Preset Folder

Saved QR presets live in this folder.

How it works:

- The backend stores QR preset data in `presets.json` here.
- This folder is separate from `/preset`, which is only for draggable image assets.
- This folder is separate from `/template`, which is only for template source images.
